/**
 * Returns the first element of array
 * @param {Array} array An arbitrary array
 * @returns {any} The first element of array or undefined if array is empty
 * 
 * @memberof module:mini-dash
 */
function head(array) {
  return array[0]
}

module.exports = head
module.exports.default = head
